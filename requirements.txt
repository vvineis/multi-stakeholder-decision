# Dependencies for the Streamlit dashboard (the deployed app).
#
# Streamlit Community Cloud auto-installs this file, so it is kept light: the
# dashboard is read-only over pre-computed result CSVs/JSONs and needs none of
# the heavy training deps. To train models / reproduce experiments, install
# requirements-training.txt instead.

streamlit>=1.30
plotly>=5.20
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
matplotlib>=3.7
jinja2>=3.0
